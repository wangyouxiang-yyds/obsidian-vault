---
date: 2026-06-02
type: experiment
stage: preprocessing
status: patch-coord-running
tags: [split-strategy, random, event-holdout, temporal, COG, VRT, mask, preprocessing]
---

# 三策略前置作業執行紀錄（2026-06-02）

## 資料現況確認（重要修正）

原計畫記載 NOAA 有 mask 場景為 395，實際核查結果：

| 類型 | 數量 | 說明 |
|------|------|------|
| NOAA 場景有 mask（可用） | **220** | 含 tile code 的新格式，可對應場景目錄 |
| NOAA mask（舊格式） | 175 | 無 tile code（如 `..._20190530_S2`），無對應場景目錄，本輪無法使用 |
| 事件場景 | **36** | 8 個事件，本次新建 mask |
| **可用合計** | **256** | |

年份分布（220 NOAA）：2019=53, 2020=49, 2021=18, 2024=34, 2025=66

---

## Step 1：事件場景標注轉換（GPKG → mask TIF）

**腳本**：`preprocess/batch_event_gpkg_to_mask.py`（本次新建）

- 掃描 MS6_sen2like 所有非 NOAA 的 2-level 場景目錄（事件場景結構與 NOAA 不同，只有 2 層）
- 自動讀取場景內 `*.gpkg`，依檔名推斷 label（oil / background / bg / others）
- 以 `*_B02.tif` 為參考 affine，多邊形填充生成 mask TIF
- JSON 存在場景資料夾內（與 GPKG 同層）

**結果**：新生成 36，失敗 0；mask/ 目錄共 431 個 TIF（395 NOAA + 36 事件）

**注意事項**：
- `_bg.gpkg`（只有希臘場景有）不被原始 keyword 識別，已加 alias 修正
- 希臘 34SGH 場景 GPKG 含多 layer（pyogrio 警告），使用預設 layer，結果正常

---

## Step 2：VRT 建置（事件場景補建）

**腳本**：`preprocess/build_vrt_ms6.py`（修改支援 2-level 事件場景）

修改內容：
- `glob('*/*/*')` 改為同時掃 `glob('*/*')`（非 NOAA 2-level）
- `build_vrt_scene` 新增 `ms6_root` 參數，自動判斷場景深度計算 location

**結果**：新建 36（事件）, 跳過 403（NOAA 已存在）；VRT 總數 **439**

---

## Step 3：COG 轉換

**腳本**：`/home/alanyh/oil_dataset/new/full_band/convert_to_cog.py`（現有）

- 掃描 MS6_sen2like 全部 band TIF（B02~B12 共 7 個波段，B01 跳過）
- `is_already_tiled` 自動跳過已轉換的 NOAA 場景
- 事件場景原始格式：strip（blockysize=1），全部需轉

**結果**：3073 個檔案處理，Failed: 0；耗時 225 分鐘

---

## Step 4：Scene Split 腳本（本次新建，3 個）

| 腳本 | 輸出目錄 |
|------|---------|
| `preprocess/build_scene_splits_random3fold.py` | `data_split/3fold_random/scene_level/` |
| `preprocess/build_scene_splits_event3fold.py` | `data_split/3fold_event/scene_level/` |
| `preprocess/build_scene_splits_temporal3fold.py` | `data_split/3fold_temporal/scene_level/` |

所有腳本：SEED=42，NUM_FOLDS=3，VAL_FRAC=0.2

### 策略一（純隨機）輸出

| Fold | Train | Val | Test |
|------|-------|-----|------|
| 1 | 117 | 29 | 74 |
| 2 | 118 | 29 | 73 |
| 3 | 118 | 29 | 73 |

### 策略二（事件留出）輸出

事件分組（test fold 對應）：
- Group A（fold 1 test）：千里達(8) + Golden Ray(2) = **10 scenes**
- Group B（fold 2 test）：帛琉(7) + 祕魯(6) + 所羅門(4) = **17 scenes**
- Group C（fold 3 test）：斯里蘭卡(4) + 模里西斯(1) + 希臘(4) = **9 scenes**

| Fold | Train（NOAA+事件） | Val（NOAA only） | Test（NOAA+事件） |
|------|------------------|-----------------|-----------------|
| 1 | 143（117+26） | 29 | 84（74+10） |
| 2 | 137（118+19） | 29 | 90（73+17） |
| 3 | 145（118+27） | 29 | 82（73+9） |

### 策略三（時序擴展）輸出

| Fold | Train | Val | Test | 說明 |
|------|-------|-----|------|------|
| 1 | 42 | 11 | 49 | train=2019前80%，val=2019後20%，test=2020 |
| 2 | 92 | 10 | 52 | train=2019+2020前80%，val=2020後20%，test=2021+2024 |
| 3 | 146 | 8 | 66 | train=2019-2021+2024前75%，val=2024後25%，test=2025 |

---

## Step 5：Patch 座標生成（進行中）

**腳本**：三個，各複製自 `generate_patch_coords_3fold.py`，僅改路徑與比例

| 腳本 | 輸出目錄 | OIL_TO_BG_RATIO |
|------|---------|----------------|
| `generate_patch_coords_random3fold.py` | `3fold_random/patch_level_GB1.5` | 1.5 |
| `generate_patch_coords_event3fold.py` | `3fold_event/patch_level_GB1.5` | 1.5 |
| `generate_patch_coords_temporal3fold.py` | `3fold_temporal/patch_level_GB1.5` | 1.5 |

參數：PATCH_SIZE=256, STRIDE=192, MIN_ANNOTATED_RATIO=0.7

**狀態**：⏳ 執行中（需手動依序跑三個腳本）

```bash
cd /mnt/backup/alanyh/oil_IR_Fullband/OIL_PROJECT_MutiBand_0422_VRT_training
conda activate mamba_env
python preprocess/generate_patch_coords_random3fold.py
python preprocess/generate_patch_coords_event3fold.py
python preprocess/generate_patch_coords_temporal3fold.py
```

---

## 待完成

- [ ] Patch 座標生成（3 個腳本，依序執行）
- [ ] YAML config（3 個，複製現有改路徑）
- [ ] 訓練啟動（3 策略 × 3 fold = 9 runs）
